# Ferramentas

Portal da Transparência: Servidor Público expõe 1 ferramenta (todas somente leitura).

### 1. `portal_transparencia_servidor_consultar`
**Input**: `cpf` (opcional), `nome` (opcional)

Portal da Transparência: Servidor Público, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Portal da Transparência: Servidor Público
```
